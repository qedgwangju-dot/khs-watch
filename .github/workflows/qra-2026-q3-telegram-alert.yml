name: QRA 2026 Q3 Telegram alert

on:
  push:
    paths:
      - ".github/workflows/qra-2026-q3-telegram-alert.yml"
      - "scripts/qra_2026_q3_alert.py"
      - "tests/test_qra_2026_q3_alert.py"
  schedule:
    # GitHub cron is UTC. Treasury release: 2026-08-05 12:30 UTC / 21:30 KST.
    - cron: "33,48 12 5 8 *"
    - cron: "3,18,33,48 13-15 5 8 *"
    - cron: "15 16-23 5 8 *"
    - cron: "15 0-3 6 8 *"
  workflow_dispatch:
    inputs:
      telegram_test:
        description: "Send a one-time Telegram route test"
        required: false
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

permissions:
  contents: write

concurrency:
  group: qra-2026-q3-telegram-alert
  cancel-in-progress: true

jobs:
  monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    env:
      TELEGRAM_TEST: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.telegram_test || 'false' }}
      EXPECTED_TELEGRAM_BOT_USERNAME: "hs8879887988798879_bot"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Validate watcher
        run: |
          python -m py_compile scripts/qra_2026_q3_alert.py
          python -m unittest tests/test_qra_2026_q3_alert.py -v

      - name: Check official Treasury QRA page
        run: python scripts/qra_2026_q3_alert.py

      - name: Publish status summary
        if: always()
        run: |
          if [ -f out/qra_2026_q3_watch.md ]; then
            cat out/qra_2026_q3_watch.md >> "$GITHUB_STEP_SUMMARY"
          else
            echo "QRA 상태 파일이 생성되지 않았습니다." >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Send Telegram alert through second bot only
        if: hashFiles('out/qra_2026_q3_alert.md') != ''
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.YEN_CARRY_TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.YEN_CARRY_TELEGRAM_CHAT_ID }}
        run: |
          if [ -z "$TELEGRAM_BOT_TOKEN" ]; then
            echo "두 번째 봇용 YEN_CARRY_TELEGRAM_BOT_TOKEN이 없습니다. 첫 번째 봇으로 대체 송출하지 않습니다." >&2
            exit 1
          fi
          if [ -z "$TELEGRAM_CHAT_ID" ]; then
            echo "두 번째 봇용 YEN_CARRY_TELEGRAM_CHAT_ID가 없습니다. 첫 번째 봇으로 대체 송출하지 않습니다." >&2
            exit 1
          fi

          python - <<'PY'
          import datetime as dt
          import json
          import os
          import pathlib
          import urllib.parse
          import urllib.request
          from zoneinfo import ZoneInfo

          token = os.environ["TELEGRAM_BOT_TOKEN"].strip()
          chat_id = os.environ["TELEGRAM_CHAT_ID"].strip()
          expected_username = os.environ["EXPECTED_TELEGRAM_BOT_USERNAME"].strip().lstrip("@")

          with urllib.request.urlopen(
              urllib.request.Request(f"https://api.telegram.org/bot{token}/getMe"),
              timeout=25,
          ) as response:
              identity = json.loads(response.read().decode("utf-8"))
          actual_username = str((identity.get("result") or {}).get("username") or "")
          if not identity.get("ok") or actual_username.lower() != expected_username.lower():
              raise RuntimeError(
                  f"잘못된 텔레그램 봇 설정: expected @{expected_username}, got @{actual_username or 'unknown'}"
              )

          title = pathlib.Path("out/qra_2026_q3_alert_title.txt").read_text(encoding="utf-8").strip()
          body = pathlib.Path("out/qra_2026_q3_alert.md").read_text(encoding="utf-8").strip()
          text = f"{title}\n\n{body}"[:4096]
          payload = urllib.parse.urlencode(
              {
                  "chat_id": chat_id,
                  "text": text,
                  "disable_web_page_preview": "true",
              }
          ).encode("utf-8")
          request = urllib.request.Request(
              f"https://api.telegram.org/bot{token}/sendMessage",
              data=payload,
              method="POST",
          )
          with urllib.request.urlopen(request, timeout=25) as response:
              result = json.loads(response.read().decode("utf-8"))
          if not result.get("ok"):
              raise RuntimeError(f"Telegram rejected message: {result}")

          confirmed = {
              "status": "confirmed",
              "bot_username": actual_username,
              "message_id": result.get("result", {}).get("message_id"),
              "confirmed_at_kst": dt.datetime.now(ZoneInfo("Asia/Seoul")).isoformat(timespec="seconds"),
          }
          pathlib.Path("out/qra_2026_q3_telegram_confirmed.json").write_text(
              json.dumps(confirmed, ensure_ascii=False, indent=2) + "\n",
              encoding="utf-8",
          )
          print(
              f"telegram_delivery_confirmed=true bot_username=@{actual_username} "
              f"message_id={confirmed['message_id']}"
          )
          PY

      - name: Persist successful production delivery
        if: success() && env.TELEGRAM_TEST != 'true' && hashFiles('out/qra_2026_q3_telegram_confirmed.json') != ''
        run: |
          python - <<'PY'
          import json
          import pathlib

          confirmed = json.loads(pathlib.Path("out/qra_2026_q3_telegram_confirmed.json").read_text(encoding="utf-8"))
          path = pathlib.Path("data/qra_2026_q3_alert_state.json")
          previous = {}
          if path.exists():
              try:
                  previous = json.loads(path.read_text(encoding="utf-8"))
              except json.JSONDecodeError:
                  previous = {}
          state = {
              "status": "sent",
              "sent_at_kst": confirmed.get("confirmed_at_kst"),
              "telegram_message_id": confirmed.get("message_id"),
              "bot_username": confirmed.get("bot_username"),
              "route": "second_alert_bot_only",
              "source": "U.S. Treasury QRA 2026 Q3",
              "corrected_from_first_bot": previous.get("status") == "pending_second_bot_resend",
              "wrong_route_sent_at_kst": previous.get("wrong_route_sent_at_kst"),
              "wrong_route_telegram_message_id": previous.get("wrong_route_telegram_message_id"),
          }
          path.parent.mkdir(parents=True, exist_ok=True)
          path.write_text(json.dumps(state, ensure_ascii=False, indent=2) + "\n", encoding="utf-8")
          PY

      - name: Upload verification artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: qra-2026-q3-alert-${{ github.run_id }}
          path: |
            out/qra_2026_q3_watch.md
            out/qra_2026_q3_alert.md
            out/qra_2026_q3_alert.json
            out/qra_2026_q3_telegram_confirmed.json
          if-no-files-found: ignore
          retention-days: 14

      - name: Commit confirmed alert state
        if: success() && env.TELEGRAM_TEST != 'true' && hashFiles('data/qra_2026_q3_alert_state.json') != ''
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/qra_2026_q3_alert_state.json
          if git diff --cached --quiet; then
            echo "QRA 알림 상태 변경 없음"
          else
            git commit -m "Record QRA 2026 Q3 second-bot delivery"
            git pull --rebase origin main
            git push
          fi
