name: HBM Monthly Export Proxy Legacy Route

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy route disabled
        run: |
          echo "This legacy monthly HBM Telegram route is disabled."
          echo "Production HBM monitoring runs through .github/workflows/rubin-hbm-telegram.yml -> scripts/samsung_hbm_watch.py."
          echo "The production route uses the Korea Customs official API month as the trigger and suppresses duplicate/stale alerts."
