name: US Army Test Range K-Defense Telegram Watch

on:
  schedule:
    # GitHub Actions는 UTC 기준. 매시간 17분에 확인.
    - cron: "17 * * * *"
  workflow_dispatch:
    inputs:
      force_notify:
        description: "현재 기준선을 저장하고 Telegram 연결 확인 메시지 전송"
        required: false
        type: boolean
        default: false
  push:
    paths:
      - ".github/workflows/us-army-test-range-kdefense-watch.yml"
      - "scripts/us_army_test_range_kdefense_watch.py"
      - ".github/us-army-test-range-kdefense-trigger"

concurrency:
  group: us-army-test-range-kdefense-watch
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  watch:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: 저장소 확인
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Python 설정
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: 의존성 설치
        run: pip install requests

      - name: 문법 확인
        run: python -m compileall -q scripts/us_army_test_range_kdefense_watch.py

      - name: 미 육군 시험장·K-미사일 웹 감시
        id: missile
        shell: bash
        env:
          FORCE_INPUT: ${{ inputs.force_notify || false }}
          FORCE_PUSH: ${{ github.event_name == 'push' && contains(github.event.head_commit.message, '[us-range-route-test]') }}
        run: |
          EXTRA=""
          if [[ "$FORCE_INPUT" == "true" || "$FORCE_PUSH" == "true" ]]; then
            EXTRA="--force-notify"
          fi
          python scripts/us_army_test_range_kdefense_watch.py $EXTRA

      - name: hs8879887988798879_bot으로 Telegram 전송
        if: steps.missile.outputs.changed == 'true'
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.YEN_CARRY_TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.YEN_CARRY_TELEGRAM_CHAT_ID }}
          FALLBACK_TELEGRAM_CHAT_ID: ${{ secrets.KHS_POLICY_TELEGRAM_CHAT_ID }}
          EXPECTED_TELEGRAM_BOT_USERNAME: hs8879887988798879_bot
          REPORT_PATH: ${{ steps.missile.outputs.report_path }}
        run: |
          if [ -z "$TELEGRAM_BOT_TOKEN" ]; then
            echo "YEN_CARRY_TELEGRAM_BOT_TOKEN이 없습니다." >&2
            exit 1
          fi

          python - <<'PY'
          import json
          import os
          import pathlib
          import requests

          token = os.environ["TELEGRAM_BOT_TOKEN"].strip()
          chat_id = (
              os.environ.get("TELEGRAM_CHAT_ID", "").strip()
              or os.environ.get("FALLBACK_TELEGRAM_CHAT_ID", "").strip()
          )
          expected = os.environ["EXPECTED_TELEGRAM_BOT_USERNAME"].strip().lstrip("@")
          report_path = pathlib.Path(os.environ["REPORT_PATH"])
          if not chat_id:
              raise RuntimeError("Telegram chat id가 없습니다.")

          me_response = requests.get(
              f"https://api.telegram.org/bot{token}/getMe",
              timeout=25,
          )
          me_response.raise_for_status()
          me = me_response.json()
          if not me.get("ok"):
              raise RuntimeError(f"Telegram getMe 실패: {me}")
          actual = str(me.get("result", {}).get("username", ""))
          if actual != expected:
              raise RuntimeError(
                  f"잘못된 Telegram 봇 경로 차단: expected=@{expected}, actual=@{actual or '확인불가'}"
              )

          text = report_path.read_text(encoding="utf-8").strip()
          if not text:
              raise RuntimeError("Telegram으로 보낼 보고서가 비어 있습니다.")

          chunks = []
          while text:
              if len(text) <= 3500:
                  chunks.append(text)
                  break
              cut = text.rfind("\n", 0, 3500)
              if cut < 1700:
                  cut = 3500
              chunks.append(text[:cut].rstrip())
              text = text[cut:].lstrip()

          message_ids = []
          for index, chunk in enumerate(chunks, start=1):
              body = (f"[{index}/{len(chunks)}]\n" if len(chunks) > 1 else "") + chunk
              response = requests.post(
                  f"https://api.telegram.org/bot{token}/sendMessage",
                  json={
                      "chat_id": chat_id,
                      "text": body,
                      "disable_web_page_preview": True,
                  },
                  timeout=30,
              )
              response.raise_for_status()
              payload = response.json()
              if not payload.get("ok"):
                  raise RuntimeError(f"Telegram 전송 실패: {payload}")
              message_ids.append(payload.get("result", {}).get("message_id"))

          confirmation = {
              "status": "confirmed",
              "bot_username": actual,
              "message_ids": message_ids,
          }
          pathlib.Path("out").mkdir(parents=True, exist_ok=True)
          pathlib.Path("out/us_army_test_range_kdefense_telegram_confirmed.json").write_text(
              json.dumps(confirmation, ensure_ascii=False, indent=2) + "\n",
              encoding="utf-8",
          )
          print(
              f"telegram_delivery_confirmed=true bot=@{actual} "
              f"messages={len(message_ids)} last_message_id={message_ids[-1]}"
          )
          PY

      - name: 기준선·보고서 저장
        if: success()
        run: |
          git config user.name "us-army-test-range-watch[bot]"
          git config user.email "us-army-test-range-watch[bot]@users.noreply.github.com"
          git add data/us_army_test_range_kdefense_state.json out/us_army_test_range_kdefense_alert.txt out/us_army_test_range_kdefense_telegram_confirmed.json 2>/dev/null || true
          if ! git diff --cached --quiet; then
            git commit -m "chore: update US Army test range K-defense watch state"
            for attempt in 1 2 3; do
              if git pull --rebase origin main && git push; then
                exit 0
              fi
              git rebase --abort 2>/dev/null || true
              sleep $((attempt * 3))
            done
            exit 1
          fi
